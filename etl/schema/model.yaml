$schema: "http://json-schema.org/draft-07/schema#"

id: Model
type: object
description: >
  A GDAN TMP model.
additionalProperties: false

required:
  - gid
  - label

properties:
  gid:
    $ref: _gids.yaml#/model_gid
  label:
    const: Model
  metadata:
    type:
      - 'null'
      - object
